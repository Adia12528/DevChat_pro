start cmd /k "cd backend && npm install && npm start"
start cmd /k "cd frontend && npm install && npm start"

@REM // stating commands for backend and frontend in separate command prompt windows. Each command prompt will navigate to the respective directory, install necessary dependencies, and start the application.