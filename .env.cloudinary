# Cloudinary Configuration
# Your credentials (for reference, NOT used in frontend code)

CLOUD_NAME=da03qqo5g
API_KEY=648475135154976
# API_SECRET=your_secret_here (NOT needed for unsigned uploads)

# ⚠️ IMPORTANT: These credentials are NOT used in the frontend code!
# The frontend only uses the cloud name embedded in App.js
# For unsigned uploads, you only need to:
# 1. Cloud name (already configured in App.js)
# 2. Upload preset "devchat_uploads" (create in Cloudinary dashboard)

# If you want to do SERVER-SIDE uploads later (more secure):
# 1. Add all three credentials to backend/.env
# 2. Install cloudinary package: npm install cloudinary
# 3. Create signed upload endpoints in server.js
